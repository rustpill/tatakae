export const BATTLE_SEED = Buffer.from("battle");
export const ESCROW_SEED = Buffer.from("escrow");
export const FIGHTER_SEED = Buffer.from("fighter");
export const CONFIG_SEED = Buffer.from("config");